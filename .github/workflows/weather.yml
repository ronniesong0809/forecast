name: Forecast

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '* * * * *'
jobs:
  daily:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Fetch weather
        run: bash ./wttr.sh
